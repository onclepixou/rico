BEGIN INPUT 
 
    INTERVAL CX(FLOAT);
    INTERVAL CY(FLOAT);
    INTERVAL R(FLOAT);
END INPUT ;


BEGIN OUTPUT  
    INTERVAL X(FLOAT);
    INTERVAL Y(FLOAT);
END OUTPUT ;

BEGIN CONSTRAINT
		E1 : R**2 = (X - CX)**2 + (Y - CY)**2;
END CONSTRAINT;
