BEGIN INPUT  
    INTERVAL X(FLOAT);
    SCALAR Y(INTEGER);
END INPUT ;

BEGIN OUTPUT  
    INTERVAL X(FLOAT);
    SCALAR Y(INTEGER);
END OUTPUT ;

BEGIN CONSTRAINT
    E1 : y = a*x + b/2;
    E2 : X = ABS(X,3*Y,sqrt(Z)) + 1/Y;
END CONSTRAINT;
