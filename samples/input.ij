BEGIN INPUT  
    INTERVAL X(FLOAT);
    SCALAR Y(INTEGER);
END INPUT ;

BEGIN OUTPUT  
    INTERVAL W(FLOAT);
    SCALAR Z(INTEGER);
END OUTPUT ;

BEGIN CONSTRAINT
    E1 : W = 2*X + 7;
    E2 : Z = cos(W) + 1/Y + max(1,2);
END CONSTRAINT;
